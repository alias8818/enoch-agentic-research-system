[extend]
useDefault = true

[[allowlists]]
description = "Known generated graph false-positive category labels; these are public prose labels, not credentials."
regexes = [
  '''Loss/gradient''',
  '''Integrity/corruption''',
  '''Duplicate/canary''',
]
